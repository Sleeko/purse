import { FilterSellerPipe } from './filter-seller.pipe';

describe('FilterSellerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSellerPipe();
    expect(pipe).toBeTruthy();
  });
});
